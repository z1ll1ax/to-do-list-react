import Category from '../types/Category';

interface CardProps {
    title?: string;
    description?: string;
    createDate?: string;
    deadlineDate?: string;
    categories?: string[];
}

export default CardProps;